import { SafeAreaView } from 'react-native'
import { View, Text } from 'react-native'
const Chat = () => {
  return (
    <SafeAreaView>
      <Text>Chat</Text>
    </SafeAreaView>
  )
}
export default Chat