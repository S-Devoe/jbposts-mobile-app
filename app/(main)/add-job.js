import { SafeAreaView } from "react-native";
import { View, Text } from "react-native";
const AddJob = () => {
  return (
    <SafeAreaView>
      <Text>AddJob</Text>
    </SafeAreaView>
  );
};
export default AddJob;
